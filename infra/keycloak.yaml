apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
type: Opaque
stringData:
  admin-password: "admin"
---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-client-secret
type: Opaque
stringData:
  secret: "change-me-in-production"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm-config
data:
  nexusgrid-realm.json: |
    {
      "realm": "nexusgrid",
      "enabled": true,
      "clients": [
        {
          "clientId": "nexusgrid-client",
          "enabled": true,
          "publicClient": false,
          "secret": "change-me-in-production",
          "redirectUris": ["http://localhost:8081/callback", "http://auth-service/callback"],
          "webOrigins": ["http://localhost:3000", "http://frontend"],
          "standardFlowEnabled": true,
          "directAccessGrantsEnabled": true
        }
      ],
      "users": [
        {
          "username": "admin",
          "enabled": true,
          "email": "admin@nexusgrid.local",
          "firstName": "Admin",
          "lastName": "User",
          "credentials": [{"type": "password", "value": "admin123", "temporary": false}],
          "realmRoles": ["default-roles-nexusgrid"]
        }
      ]
    }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
      - name: keycloak
        image: quay.io/keycloak/keycloak:24.0
        args: ["start-dev", "--import-realm"]
        ports:
        - containerPort: 8080
        env:
        - name: KEYCLOAK_ADMIN
          value: "admin"
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-secret
              key: admin-password
        - name: KC_HOSTNAME_URL
          value: "http://localhost:8180"
        - name: KC_HOSTNAME_ADMIN_URL
          value: "http://localhost:8180"
        - name: KC_HOSTNAME_STRICT
          value: "false"
        - name: KC_HOSTNAME_STRICT_HTTPS
          value: "false"
        - name: KC_HTTP_ENABLED
          value: "true"
        - name: KC_PROXY
          value: "edge"
        volumeMounts:
        - name: realm-config
          mountPath: /opt/keycloak/data/import
        - name: keycloak-data
          mountPath: /opt/keycloak/data/h2
        readinessProbe:
          httpGet:
            path: /realms/master
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /realms/master
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 20
          failureThreshold: 5
          timeoutSeconds: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      volumes:
      - name: realm-config
        configMap:
          name: keycloak-realm-config
      - name: keycloak-data
        persistentVolumeClaim:
          claimName: keycloak-data
---
apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  selector:
    app: keycloak
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
